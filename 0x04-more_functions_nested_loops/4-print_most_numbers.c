#include "main.h"

/**
 *print nubers - print 2-4
 use _putchar twice
 * Return: Always 0.
 */
int main(void)
{
	if num;
	for (num <= 9 || num = 0; num++)
	{
		if (num == 2 || num == 4)
			continue;
		_putchar(num + 48);
	}
	_putchar('\n');
